# Отчёт о приёмке — Этап 1 (Data & Score Engine)

Проект: **MVP AI-системы для свитч-трейдинга** (futuguru).
Этап 1 по КП: **95 000 ₽ / 10 рабочих дней**, подэтапы 1.1–1.10.
Дата отчёта: 2026-09-19.

---

## 1. Резюме

Все 10 подэтапов Этапа 1 реализованы и покрыты автотестами. Проверяемые критерии
готовности (DoD) сформулированы как код и фактически прогнаны — не «на глаз».

- ✅ **127 автотестов** — все проходят.
- ✅ Линтер (ruff) — чисто.
- ✅ По каждому подэтапу сохранён артефакт проверки в `docs/acceptance-evidence/<подэтап>/`.

Часть проверок требует реальных API-ключей и сервера — они помечены **«Stage 0»** и
закрываются после того, как заказчик предоставит ключи (FMP/Alpaca/Finnhub/Telegram/
LLM) и VPS. Вся бизнес-логика, расчёты и сквозной сценарий проверены на настоящих
компонентах с мокированием только сетевых вызовов.

Как перепроверить самостоятельно:
```bash
pip install -e ".[dev]"
pytest -q          # 127 passed
```

---

## 2. Результаты по подэтапам (нумерация = чек-лист заказчика)

| № | Подэтап | Статус | Автотесты |
|---|---|---|---|
| 1.1 | Источники данных (FMP/SEC/Alpaca/Finnhub) + переключение на резерв | ✅ Готово | 20 |
| 1.2 | Получение и качество данных (нормализация, дедуп, политика пропусков) | ✅ Готово | 12 |
| 1.3 | Расчёт метрик (RSI, MACD, EMA, ATR, Volume Ratio, Rel.Strength) | ✅ Готово | 20 |
| 1.4 | Семь факторных Score | ✅ Готово | 15 |
| 1.5 | Итоговый Final Score + версионирование формул | ✅ Готово | 11 |
| 1.6 | Базовый Risk Filter (6 флагов) | ✅ Готово | 14 |
| 1.7 | База данных и история сигналов | ✅ Готово | 6 |
| 1.8 | Мониторинг источников и ошибок (`/health`) | ✅ Готово | 6 |
| 1.9 | Базовый Telegram-интерфейс | ✅ Готово | 16 |
| 1.10 | Сквозная контрольная проверка (демо Этапа 1) | ✅ Готово | 3 |

---

## 3. Детализация DoD

### 1.1 Источники данных
- ✅ Клиенты FMP, SEC EDGAR, Alpaca, Finnhub; каждый проставляет источник и время получения данных.
- ✅ `source_router` переключается с FMP на резерв (Alpaca — цены, Finnhub — новости) при ошибке.
- ⏳ **Stage 0:** прогон по реальному тикеру (AAPL) на живом API-ключе.

### 1.2 Получение и качество данных
- ✅ Нормализация OHLCV (единая торговая дата America/New_York), дедупликация по (тикер, дата).
- ✅ Валидация ответов; отсутствующие значения хранятся как «нет данных» и помечаются — **никогда не подменяются нулём или выдуманным значением**.
- ✅ Контрольные сценарии (некорректный ответ, timeout, rate limit, недоступный источник, пропуск) проходят без падения и без ложного расчёта.

### 1.3 Расчёт метрик
- ✅ Все индикаторы реализованы вручную (полный контроль над формулой и воспроизводимость).
- ✅ Unit-тесты на эталонном ряде (±0.01), включая ручную сверку рекуррент RSI и ATR по Уайлдеру.
- ⏳ **Stage 0:** сверка ≥ 3 метрик с независимым источником (TradingView) на живом тикере.

### 1.4 Семь факторных Score
- ✅ 7 Score по формулам ТЗ; по каждому видна цепочка «входные метрики → правило → оценка».
- ✅ Веса Final Score = 20/15/15/15/10/10/15.

### 1.5 Итоговый Score
- ✅ Final Score всегда в диапазоне 0–100; округление один раз на выходе.
- ✅ Воспроизводимость: повторный расчёт с теми же данными даёт идентичный результат.
- ✅ Тест чувствительности пройден; версия формул фиксируется с каждым расчётом.

### 1.6 Базовый Risk Filter
- ✅ 6 флагов (высокая волатильность, близость отчётности, гэп, перегретость, ликвидность, неполные данные), каждый — с причиной и конкретным значением.
- ✅ Критичный риск понижает допустимый статус; неполные данные всегда дают высокий уровень риска.

### 1.7 База данных и история
- ✅ Все таблицы схемы; история сигналов — только добавление (append-only).
- ✅ Два расчёта одного тикера создают две записи; по любой записи восстанавливается цепочка от входных данных до Final Score.
- ⏳ **Stage 0:** применение миграций на реальном PostgreSQL (docker/VPS).

### 1.8 Мониторинг источников
- ✅ `/health` показывает по каждому источнику: статус, время последнего успеха, задержку, счётчик и причину ошибок.
- ✅ Искусственный сбой виден в `/health`; после восстановления статус обновляется автоматически.

### 1.9 Базовый Telegram-интерфейс
- ✅ Приём тикера → метрики, 7 Score, Final Score, риск-флаги; `/history`; кнопки Подробнее/Метрики/Risk/История.
- ✅ Неизвестный тикер → понятная ошибка без падения; техническая ошибка **не превращается** в торговый вывод.
- ✅ Параллельные запросы по разным тикерам не смешиваются.
- ⏳ **Stage 0:** запуск живого бота (токен) и ручной клик со скриншотом.

### 1.10 Сквозная проверка (демонстрация Этапа 1)
- ✅ Один тикер проходит всю цепочку: источник → метрики → 7 факторов → Final Score → Risk → БД → Telegram.
- ✅ Значения в backend и в Telegram совпадают; повторный расчёт идентичен.
- ✅ Отказ основного источника обрабатывается резервом; полная недоступность → честное сообщение (ложный расчёт не сохраняется).
- ⏳ **Stage 0:** тот же прогон на реальном API-ключе и живом тикере.

---

## 4. Что нужно от заказчика для закрытия «Stage 0»

Чтобы снять оставшиеся отметки ⏳ и провести финальную демонстрацию на живых данных:
1. API-ключи: FMP (тариф Starter+), Alpaca, Finnhub.
2. Токен Telegram-бота (@BotFather).
3. Ключ LLM (Anthropic и/или OpenAI) — для Этапа 2.
4. VPS (Ubuntu 22.04+, Docker) с доступом по SSH.

Инструкция по вводу ключей и запуску — в `docs/SETUP.md`.

---

## 5. Приложения (доказательства)

По каждому подэтапу — вывод тестов, демонстрации и статус DoD:
`docs/acceptance-evidence/1.1/` … `docs/acceptance-evidence/1.10/`.

Пример сквозной демонстрации (подэтап 1.10): по тикеру AAPL Final Score в Telegram
и в базе данных совпадают, повторный расчёт из сохранённого снимка даёт то же
значение — см. `docs/acceptance-evidence/1.10/evidence.md`.
